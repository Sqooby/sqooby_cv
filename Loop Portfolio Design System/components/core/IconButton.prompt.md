Round, outlined, icon-only button. Use for nav menu toggles, "+" expanders and social links.

```jsx
<IconButton name="plus" label="More links" tone="paper" size="md" />
```

Always pass `label`. Icon name is any Lucide kebab-case name.
