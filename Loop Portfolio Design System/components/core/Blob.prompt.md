Atmosphere, not content. Place 1–3 behind a hero or section with `position:absolute` and low opacity.

```jsx
<Blob palette="sunset" size={620} opacity={.7} style={{ position:'absolute', top:-120, right:-80, zIndex:0 }} />
```
