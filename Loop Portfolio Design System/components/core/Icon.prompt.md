Thin wrapper over the Lucide static icon CDN — the only icon source in Loop.

```jsx
<Icon name="arrow-up-right" size={18} />
<Icon name="arrow-right" tone="inverse" />
```

`tone="inverse"` inverts the SVG for use on ink/blue backgrounds. Never hand-draw an icon; pick the nearest Lucide name.
