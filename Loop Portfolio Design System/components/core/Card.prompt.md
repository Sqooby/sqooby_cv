The base surface. Everything boxed in Loop is a Card or built on one.

```jsx
<Card tone="butter" padding="var(--space-10)">…</Card>
```

Use `shadow="soft"` only for floating/3D contexts; the default hard offset is the brand look.
