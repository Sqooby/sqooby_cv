Page header. Floats over content with a blur; never full-bleed.

```jsx
<NavBar brand="Loop" items={[{id:'work',label:'Work'},{id:'about',label:'About'}]} activeItem="work" onNavigate={setSection} />
```
