Wraps every image. Without `src` it renders a brand-gradient placeholder, so layouts stay honest before assets arrive.

```jsx
<MediaFrame palette="sunset" ratio="16 / 10" label="Case study cover" />
```
